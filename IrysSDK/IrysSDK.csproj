<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="NSwag.MSBuild" Version="14.6.3" PrivateAssets="all">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <Target Name="NSwag" BeforeTargets="Compile">
    <Exec Command="$(NSwagExe) openapi2csclient /input:openapi.yml /output:BaseSdk.gen.cs /className:InternalBaseSdk /namespace:IrysSDK /JsonLibrary:SystemTextJson" />
  </Target>

</Project>
